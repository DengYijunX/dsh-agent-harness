# 变更记录 #004 — 2026-09-03

## 概要

| 标签 | 等级 | 根因 | 方案 |
|------|------|------|------|
| feature, refactor, frontend | 🟡 中 | 初版原型偏营销页布局，用户任务不够突出，用户态和开发态视觉割裂 | 重构为桌面端优先的统一 App Shell，用户端聚焦任务与结果，开发端聚焦事件观测 |

## 涉及文件

- Modify: `apps/web/src/App.tsx` — 重做导航、用户工作区和开发工作台交互
- Modify: `apps/web/src/styles.css` — 重做尺寸、间距、布局、色彩和响应式规则

## 核心改动

- 新增统一窄侧栏和顶部 App Header，适配未来桌面容器。
- 用户面板改为任务中心布局，减少装饰性大留白，历史任务支持切换。
- 用户端改为中文产品文案，移除 `MOCK ENV` 等内部实现提示。
- 开发面板增加会话列表、事件时间线和可点击 Raw 事件详情。
- 用户面板与开发面板共享品牌色和 Shell 结构，只在信息密度和调试内容上区分。
- 移除外部 Google Fonts 依赖，改用本地系统字体栈，更适合桌面端和离线环境。

## 验证

- 命令: `npm test -- --run`
- 结果: 2 个测试文件、3 个测试全部通过 ✅
- 命令: `npm run typecheck`
- 结果: TypeScript 检查通过 ✅
- 命令: `npm run build`
- 结果: Vite 生产构建成功 ✅
